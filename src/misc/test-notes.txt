setup/teardown - nastaveni, erase pred/po testu
"test fixture state"

test on all levels

pickling and unpickling - serialization :)

mind maps
flash demos

test list && automated generating of test maps

for long-live tests - do not interact with enviroment

pyUnit
- the same as unittest

unittest 
- xUnit implementation
- std library >=2.1
- inherit unittest.TestCase
- setUp tearDown methods + methods start with test

doctest
- std library >=2.1
- exact match, no white lines, tight syntax

py.test
- inside py lib
- no api, functions test_ files test_ classes Test_
- hooks called setup_module/teardown_module, setup_class/teardown_class and setup_method/teardown_method
- recursive calling in subdirs
- lot o cmd line switches
- disabled atributes according to tests (disabled = arch == mips)
- generative tests



testosterone - interactive gui for unittest

testresources - resource managing for tests
testoob
- regexp filtering of tests (unittest)
- xml / html output
- python debugger snapshots
- threaded (no synchro), repeating (and probability computing)


python - if __main__ clause

