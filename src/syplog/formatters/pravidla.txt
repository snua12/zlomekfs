typedef uint32_t fibheapkey_t;

navratova_hodnota funkce (prom, prom)

/*! Comment
 @param param
 @return return
*/
typedef struct vole_vole_def 
{
  /// comment on part
  int part;

} * vole_vole;

dve mezery indent

files with - (dash)

expensive checks of in params in ENABLE_CHECKING

on functions:
in .c brief description ///
in .h long description /*! */

if no .h header, long description in .c too
for inlines in .h short description in .h too